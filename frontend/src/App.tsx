import { useAuthenticated } from "./lib/hooks/useAuthenticated";
import { Button } from "./components/ui/button";

function App() {
  //useAuthenticated();

  return (
    <div>
      <Button>Test</Button>
    </div>
  );
}

export default App;
